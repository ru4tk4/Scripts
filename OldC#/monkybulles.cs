using UnityEngine;
using System.Collections;

public class monkybulles : MonoBehaviour {
	public GameObject bulles;
	public GameObject firehere;
	public float speed ;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	/*}
	public void Bullefire(){
		Rigidbody shoot   =   Instantiate(bulles,firehere.transform.position, firehere.transform.rotation) as Rigidbody ;

			
		shoot.velocity = transform.TransformDirection(Vector3( 0, 0, speed));*/
	}
}
